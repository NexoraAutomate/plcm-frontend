/**
 * @deprecated Import from `@/components/app-fullscreen` instead.
 * Re-export kept so existing executive dashboard imports keep working.
 */
export {
  AppFullscreenProvider as ExecutivePresentationProvider,
  useAppFullscreen as useExecutivePresentation,
  useAppFullscreen,
} from '@/components/app-fullscreen';
